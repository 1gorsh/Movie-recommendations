<b>{{ recommendation.name }}</b>, showings at
{% for showing in recommendation.showings %}
    {{ showing|date('h:i a') }}{% if not loop.last %}, {% endif %}
{% endfor %}
<br>
rating: {{ recommendation.rating }}, genres: <i>{{ recommendation.genres|join(', ') }}</i>
