Name: {{ recommendation.name }}<br>
rating: {{ recommendation.rating }}<br>
genres: {{ recommendation.genres|join(', ') }}<br>
showings:
{% for showing in recommendation.showings %}
    {{ showing|date('H:i:s') }}{% if not loop.last %}, {% endif %}
{% endfor %}
